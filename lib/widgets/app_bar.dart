import 'package:flutter/material.dart';

class NSUTAppBar extends StatelessWidget {
  const NSUTAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AppBar();
  }
}
